version: '3'
services:
  db:
    image: mariadb
    env_file: .env
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0' -e production
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    depends_on:
      - db
